const $=(s,c=document)=>c.querySelector(s);const $$=(s,c=document)=>[...c.querySelectorAll(s)];
const theme=localStorage.getItem('theme')||'light';document.documentElement.dataset.theme=theme;
function setTheme(t){document.documentElement.dataset.theme=t;localStorage.setItem('theme',t);$$('[data-theme-toggle]').forEach(b=>b.textContent=t==='dark'?'☀️':'🌙')}
function renderCards(selector,items){const el=$(selector);if(!el||!items)return;el.innerHTML=items.map(i=>`<article class="card reveal"><span class="tag">${i.category||'ইসলাম'}</span>${i.arabic?`<p class="arabic">${i.arabic}</p>`:''}<h3>${i.title}</h3><p>${i.text}</p>${i.source?`<p class="source">— ${i.source}</p>`:''}</article>`).join('')}
function allItems(){return [...SITE_CONTENT.hadith.map(x=>({...x,type:'হাদিস'})),...SITE_CONTENT.quran.map(x=>({...x,type:'কুরআন'})),...SITE_CONTENT.articles.map(x=>({...x,type:'প্রবন্ধ'}))]}
function wireSearch(){const input=$('#site-search'),out=$('#search-results');if(!input||!out)return;const go=()=>{const q=input.value.trim().toLowerCase();if(!q){out.innerHTML='';return}out.innerHTML=allItems().filter(i=>(i.title+i.text+i.category+(i.arabic||'')).toLowerCase().includes(q)).map(i=>`<article class="card"><span class="tag">${i.type} • ${i.category}</span><h3>${i.title}</h3><p>${i.text}</p></article>`).join('')};input.addEventListener('input',go)}
function boot(){renderCards('[data-render="hadith"]',SITE_CONTENT.hadith);renderCards('[data-render="quran"]',SITE_CONTENT.quran);renderCards('[data-render="articles"]',SITE_CONTENT.articles);wireSearch();$$('[data-theme-toggle]').forEach(b=>{b.textContent=document.documentElement.dataset.theme==='dark'?'☀️':'🌙';b.addEventListener('click',()=>setTheme(document.documentElement.dataset.theme==='dark'?'light':'dark'))});const menu=$('#mobile-menu');$('#menu-toggle')?.addEventListener('click',()=>menu?.classList.toggle('open'));const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});$$('.reveal').forEach(e=>obs.observe(e));}
document.addEventListener('DOMContentLoaded',boot);
